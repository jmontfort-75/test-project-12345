name: Build and Push Staging

on:
  push:
    branches: [staging]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.frontend
          push: true
          tags: jmontfort/test-project-12345-frontend:staging
          build-args: |
            NEXT_PUBLIC_API_URL=https://api.test-project-12345-staging.projectsin.ai
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.backend
          push: true
          tags: jmontfort/test-project-12345-backend:staging
          cache-from: type=gha
          cache-to: type=gha,mode=max


      - name: Trigger Coolify staging frontend
        env:
          COOLIFY_WEBHOOK: ${{ secrets.COOLIFY_WEBHOOK_STAGING_FRONTEND }}
          COOLIFY_TOKEN: ${{ secrets.COOLIFY_TOKEN }}
        run: |
          if [ -z "$COOLIFY_WEBHOOK" ]; then
            echo "Skipping Coolify deploy: COOLIFY_WEBHOOK_STAGING_FRONTEND is not set yet."
            exit 0
          fi

          curl --fail --silent --show-error --max-time 30 -X GET "$COOLIFY_WEBHOOK" \
            -H "Authorization: Bearer $COOLIFY_TOKEN"


      - name: Trigger Coolify staging backend
        env:
          COOLIFY_WEBHOOK: ${{ secrets.COOLIFY_WEBHOOK_STAGING_BACKEND }}
          COOLIFY_TOKEN: ${{ secrets.COOLIFY_TOKEN }}
        run: |
          if [ -z "$COOLIFY_WEBHOOK" ]; then
            echo "Skipping Coolify deploy: COOLIFY_WEBHOOK_STAGING_BACKEND is not set yet."
            exit 0
          fi

          curl --fail --silent --show-error --max-time 30 -X GET "$COOLIFY_WEBHOOK" \
            -H "Authorization: Bearer $COOLIFY_TOKEN"
